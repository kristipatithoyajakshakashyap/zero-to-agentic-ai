# Agentic AI Track - Environment Template
# Copy this file to `.env` inside 03_agentic_ai/ and fill in your keys.
# NEVER commit the real .env file (it is gitignored).

# Required for module 02 notebook 02 (fast free-tier cloud inference)
GROQ_API_KEY=

# Optional - unlocks HuggingFace-hosted model inference (local HF embeddings work without it)
HUGGINGFACEHUB_API_TOKEN=

# Optional - used ONLY inside 01_chat_models_providers/04_openai_and_abstraction.ipynb
OPENAI_API_KEY=
