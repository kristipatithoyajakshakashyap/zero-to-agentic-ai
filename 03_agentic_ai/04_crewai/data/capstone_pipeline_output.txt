This project is a full-stack Todo application designed to help users track tasks by creating, updating, filtering, and deleting items through an intuitive interface. It features a React frontend with modular components for list management and form input, backed by a RESTful API that persists data in a relational database. To run the application, clone the repository, install the dependencies for both the frontend and backend, and start the development servers to access the local instance.